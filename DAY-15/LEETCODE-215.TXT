class Solution {
public:
    int findKthLargest(vector<int>& nums, int k) {
      priority_queue<int>har;
      for(int i=0;i<nums.size();i++){
         har.push(nums[i]);
      }
      while(k>1){
        har.pop();
        k--;
      }
        return har.top();
    }
};