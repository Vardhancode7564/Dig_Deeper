class Solution {
public:
    string largestOddNumber(string num) {
        int n=num.length();
        for(int i=n-1;i>=0;i--){
            if(num[i]=='0'||num[i]=='2'||num[i]=='4'||num[i]=='6'||num[i]=='8')num.pop_back();
            else return num;
        }
        return num;
    }
};